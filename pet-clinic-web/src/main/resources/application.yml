spring:
  profiles:
    active: springdatajpa, debug
  jpa:
    show-sql: false
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            scripts:
#              action: create-drop
#              create-target: "create.sql"
#              drop-target: "drop.sql"
  h2:
    console:
      enabled: false
  datasource:
    url: 'jdbc:h2:mem:testdb'
    platform: h2
